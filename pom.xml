<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>com.theglitch</groupId>
    <artifactId>theglitch-parent</artifactId>
    <version>1.0.0</version>
    <packaging>pom</packaging>

    <name>The Glitch - Parent</name>
    <description>Parent POM for all Glitch plugins — single source for Java version, Paper API, and build plugins</description>

    <properties>
        <!-- Single place to bump Java / Paper for ALL 8 plugins -->
        <java.version>21</java.version>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <paper.version>1.21.4-R0.1-SNAPSHOT</paper.version>
        <vault.version>1.7</vault.version>
        <papi.version>2.11.10</papi.version>
        <oraxen.version>1.218.0</oraxen.version>
        <fancynpcs.version>2.0.0</fancynpcs.version>
        <mythicmobs.version>5.13.0</mythicmobs.version>
        <luckperms.version>5.4</luckperms.version>
        <maven.compiler.plugin.version>3.13.0</maven.compiler.plugin.version>
        <maven.jar.plugin.version>3.4.2</maven.jar.plugin.version>
        <maven.shade.plugin.version>3.5.1</maven.shade.plugin.version>
    </properties>

    <modules>
        <module>plugins/GlitchCommon</module>
        <module>plugins/GlitchRaid</module>
        <module>plugins/GlitchItems</module>
        <module>plugins/GlitchClasses</module>
        <module>plugins/GlitchShops</module>
        <module>plugins/GlitchStash</module>
        <module>plugins/GlitchHideout</module>
        <module>plugins/GlitchDeathRules</module>
        <module>plugins/GlitchHealthBar</module>
        <module>plugins/GlitchDungeons</module>
        <module>plugins/GlitchInsurance</module>
        <module>plugins/GlitchEvents</module>
        <module>plugins/GlitchLoot</module>
    </modules>

    <repositories>
        <repository>
            <id>papermc</id>
            <url>https://repo.papermc.io/repository/maven-public/</url>
        </repository>
        <repository>
            <id>jitpack</id>
            <url>https://jitpack.io</url>
        </repository>
    </repositories>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>io.papermc.paper</groupId>
                <artifactId>paper-api</artifactId>
                <version>${paper.version}</version>
                <scope>provided</scope>
            </dependency>
            <dependency>
                <groupId>net.luckperms</groupId>
                <artifactId>api</artifactId>
                <version>${luckperms.version}</version>
                <scope>provided</scope>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <build>
        <pluginManagement>
            <plugins>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-compiler-plugin</artifactId>
                    <version>${maven.compiler.plugin.version}</version>
                    <configuration>
                        <source>${java.version}</source>
                        <target>${java.version}</target>
                    </configuration>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-jar-plugin</artifactId>
                    <version>${maven.jar.plugin.version}</version>
                    <configuration>
                        <finalName>${project.artifactId}-${project.version}</finalName>
                    </configuration>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-shade-plugin</artifactId>
                    <version>${maven.shade.plugin.version}</version>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-resources-plugin</artifactId>
                    <version>3.3.1</version>
                </plugin>
            </plugins>
        </pluginManagement>
        <resources>
            <resource>
                <directory>src/main/resources</directory>
                <filtering>true</filtering>
            </resource>
        </resources>
    </build>
</project>

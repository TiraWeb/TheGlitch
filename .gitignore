__pycache__/
*.pyc

# Maven / Java
target/
*.jar
!server/plugins/Oraxen/pack/textures/*.png
*.class
*.log
hs_err_pid*

# Plugin lib jars (system-scope, live-only; seeded by build.sh)
plugins/*/lib/
server/plugins/*.jar
server/plugins/*.jar.*
server/plugins/Glitch*/config.yml
server/plugins/Glitch*/messages.yml
server/plugins/Glitch*/shops.yml

# IDE
.idea/
.vscode/
*.iml
.classpath
.project
.settings/

# OS
.DS_Store
Thumbs.db

# Build / deploy artefacts
build/
logs/
*.schem
*.nbt

# Local overrides
server/logs/
server/world/
server/world_nether/
server/world_the_end/

try2.key

# Backups (worlds+data archives pulled from host)
backups/*.tar.gz
backups/*.tar
backups/*.sha256

# Downloaded world save archives (deployed to the host out-of-band, never committed)
/*.zip
!backups/.gitkeep

# Downloaded MythicMobs/ModelEngine content packs (source art, extracted at repo
# root) — only the specific blueprint/mob/skill files we pick from them are
# ever committed, under server/plugins/. See docs/MODELS.md.
/Beyond Mob Pack*/
/EndMobsVol1*/
/the_voids/

# Purchased dungeon maps/boss packs — licensed, never commit (public repo)
/Dungeon_bosses/
/Dungeon_maps.zip
/dungeon-private/

# The Glitch - Current Status

> Authoritative implementation status. Updated 2026-09-22.
>
> This file distinguishes repository evidence from live-server verification.
> A source implementation or configuration is not considered launch-ready until
> it has been built, deployed, and tested on the server.

## Status Labels

- **Implemented:** present in source or configuration and reviewed against the repository.
- **Source-only:** source exists but deployment or runtime testing is incomplete.
- **Configured:** scripts/configuration exist, but live state still needs verification.
- **Live-only:** generated or uploaded server data is not stored in this repository.
- **Partial:** the core exists but important features or integrations are missing.
- **Planned:** design exists; implementation has not started.
- **Blocked:** dependent on external assets, live access, or an unresolved technical decision.

## Overall

The server has a solid foundation: the full Phase 5.9 plugin set exists in source — extraction vault **+ dynamic random validated arenas**, classes with full abilities/ultimates, GlitchHUD per-world sidebar, item V1 with Residual consumers, merchants, containers, Red Zone population, death rules, hideout progression, raid lifecycle, insurance, world events, and smart loot. All **12** deployable plugins + shared `GlitchCommon` build through the 14-module Maven reactor (`scripts/build-all.sh`) and are deployed with clean logs (2026-09-01 — `f312262` GlitchHUD icon sidebar + TAB takeover, `c1634b3`/`9d8f05a`/`a0edffa` dynamic extraction + force-loaded marker chunks + tightened `SpotPicker` flatness, `c9a229e` ping/TPS fallback + `ByteTag` PDC crash fix, plus earlier Folia wrapper + real `PlaceholderExpansion` + shop atomicity + `d488a7d` PAPI repo + Coins account-bound reload). 2026-09-02 — `4d8c554+b638e45` economy+balance via `scripts/deploy-balance-2026-09-02.sh` (`rift_vault=6` scatter confirmed) + `f1da4d0` armor rework via `scripts/deploy-armor-2026-09-02.sh` (RCON armor verified, service active). Cycle `3/3 at (670,299),(69,569),(299,942)` verified live. Main gaps: in-game playtests of newer plugins, Identifier NPC flow, physical world content, GlitchDungeons (deferred), and launch operations. **2026-09-14:** custom ModelEngine rigs live for Glitch Warden (walk cycle + planted feet) and Glitch Wisp (winged glide) — visual sign-off pending; pre-alpha hardening (raid-buffer red-entry block, dialog removal, floating shop/dungeon panels) + rank ladder deployed. **2026-09-22: the module/plugin counts above are now stale** — GlitchDungeons, GlitchHUD, GlitchHealthBar, and GlitchLoot were removed from the reactor entirely (9 deployable + `GlitchCommon` = 10 modules now), replaced by MythicDungeons, MythicHUD, MythicMobs' native `HealthBar` field, and plain MythicMobs DropTables respectively; see the dated entry below.

## Area Status

| Area | Status | Reality |
|---|---|---|
| Server bootstrap | Implemented | Purpur, Java, systemd, firewall, plugin installation, and config seeding are scripted. |
| Reproducible deployment | Partial | Custom plugins are not built by `bootstrap.sh`; external world saves, generated arenas, and some plugin jars remain live-only. |
| Java/Bedrock platform | Configured | Geyser/Floodgate configuration exists; a real Bedrock join test is pending. |
| World mechanics | Configured | World rules, borders, and protection scripts exist. The actual imported maps are external/live-only; the default setup can create generated worlds. |
| HUD/sidebar (MythicHUD) | Live (scoreboard only) | **2026-09-22: GlitchHUD removed entirely** (per-world sidebar, below-name stacks, TAB takeover, negative_space font shifts — all gone with it). Replaced by MythicHUD; the TAB scoreboard (restyled to match GlitchHUD's title gradients/glyphs/star meter) is live and confirmed working. **2026-09-22 (round 5): removed the imported "Volya's MMORPG HUD Bundle" content entirely** (main resource bar, skill bar, party HUD) per operator decision, after the skill bar/resource bar/party HUD all needed repeated live-playtest fixes across rounds 3-4 (title-rune scoreboard, health-bar tracking, party-plugin bridge, etc. — see git history) and the operator decided it wasn't worth keeping. `MythicHUD`'s `layout: default: []` now, so it renders nothing; the plugin stays installed for a possible future config. This also removed the **Parties** plugin and GlitchRaid's `PartiesBridge` (installed solely to support the party HUD in round 3 — see the removed HANDOFF.md section for that history) since nothing consumes them anymore. **2026-09-22 (round 7): round 5's removal was incomplete** — it left ~66 texture files behind that the Volya bundle used to blank out the real vanilla hunger/health/air/armor/XP icons (so its own bars could visually replace them), which with the bars themselves gone made the vanilla HUD invisible instead of restoring it. Deleted the override textures too; vanilla HUD is back. Only the TAB scoreboard remains as the live custom HUD element. |
| GlitchStash | Implemented | Extraction vault, YAML persistence, retrieval GUI, overflow preservation, Multiverse teleport. Dynamic extraction integrated (`DynamicExtractionManager`/`SpotPicker`/`WaypointBridge`/`ExtractionMarkers`), variant key zones auto-follow, force-loaded marker chunks, `scatter center (1000,1000) r1000` land fix (`cd74932`), `ByteTag` PDC crash guarded (`c9a229e`). |
| Standard extraction | Implemented (dynamic) | 30s capture. `AutoExtractScheduler` 31m cycle (`36000` ticks open + `5s` scatter) → `DynamicExtractionManager` picks 3 validated random `extraction_dyn*` arenas (`SpotPicker` 250×12-deep, 9-point tol2, 2-deep `isOccluding`, 30-block separation, WorldGuard-aware, `CuboidRegion y-1..y+4`). Live `Cycle #1 3/3 at (670,299),(69,569),(299,942)` on `2026-09-01`; particles ring at `r*0.6` + flare + `TextDisplay`. In-game verified: capture holds inside the region at all 3 points + ring particles render. |
| Fast/Silent extraction | Implemented | GlitchStash variant zones + key consumption + arming + payout bonus (`/extractadmin`), now also auto-follow dynamic points (universal keys). Static VelKoth `extract_fast` (15s)/`extract_silent` (10s) arenas still creatable manually and mirrored into `extraction-variants.zones`. |
| GlitchClasses | Partial | Four classes, persistence, GUI, and keybind-activated abilities exist (F prime / Sneak+F tactical / Sneak+Q ultimate, game worlds only — ability items removed). Starter kit on first class select added in source 2026-08-06. Abilities completed in source 2026-08-10: all four ultimates, missing traits (Vigilance, Scavenge tag hook, Resonance Surge, Engineer repair), EMP impact, cloak break, Ironclad knockback fix, Revive Beacon healing, real reset costs. Built + deployed; loads clean on live (2026-08-21). In-game playtest pending. |
| GlitchHideout | Built (playtest pending) | GlitchHideout plugin (2026-08-10): 7 stations with shard costs + prerequisites, per-player persistence, workbench crafting (ITEM_SYSTEM §7), extended stash + armory storage with auto-sort, med heal, /hideout GUI. Built via reactor and deployed; loads clean on live (2026-08-21). **2026-09-20:** Intel Center's hostile-glow effect removed (player feedback: looked weird) — station is now a pure prerequisite gate for Arcane Core/Workbench 3. Physical hub building is in-game work. |
| MythicMobs | Partial | Ten mob definitions (2026-08-03) and per-tier drop tables exist in repo. Red Zone spawn areas seeded (2026-08-10); dungeon-slot spawners via setup-dungeon-regions.sh. Live test pending. |
| Custom mob models (ModelEngine) | Source-only (deploy pending) | 2026-09-21: full-adopt custom rigs for all 8 non-boss mobs + 2 new mobs (GlitchReaver mini-boss, GlitchHarrower), sourced from 3 downloaded packs (Beyond Mob Pack, the_voids, EndMobsVol1). 10/12 free-tier slots used. GlitchCore and TheGlitchKing both removed 2026-09-22 (see below). See docs/MODELS.md for the full roster and docs/STATUS.md's dated entry below for the deploy record. Visual/animation sign-off pending. |
| Oraxen items | Configured | 20 item definitions (`displayname:` → `itemname:`), textures, and lore are present; Oraxen must be built/deployed on the server. |
| GlitchItems | Implemented | Deployed and live-tested (gear rolls, `/identify`). Residual Glitch consumers added in source: loot luck applies at identify (star-luck + rarity surge), elite hunt spawns MythicMobs elites at 5+ stacks (2026-08-06); container loot-luck consumer added 2026-08-10. |
| Loot containers | Built (playtest pending) | GlitchItems container system (2026-08-10): Debris Pile / Loot Cache / Vault / Rift Vault, per-type rarity tables, key consumption, per-block regen cooldown, loot-luck consumer, `/glitchcontainers` admin tools. Built + deployed; container keys (need-key vs open) re-verified in-game 2026-09-01 after the `ByteTag` guard; in-game marking and full playtest remain. |
| GlitchShops | Implemented | Deployed and live-tested (`/shop` buy/sell, atomic Vault transactions 2026-08-23 — deposit-first sell + withdraw `transactionSuccess` + refund on failure). Grand Bazaar NPC placement and balance tuning remain. |
| Mob health bars | Implemented | **2026-09-22 (later): switched from MythicMobs' native per-mob `HealthBar` field to the `littleroom healthbar` MythicMobs pack** (`server/plugins/MythicMobs/Packs/littleroom healthbar/`) — the native field only drew a bar once a mob was damaged and was easy to miss; the new pack is a global-listener-driven text-display system with configurable style, status effects, and damage numbers (also shows above players below 99% HP — a real PvP-visibility change). The `HealthBar:` block was removed from all 10 roster mob files. The pack's listener is world-scoped, so one `HEALTHBAR_GLOBAL_LISTENER` is spawned per active world (hub/glitch_pve/glitch_red/eleria/horizons) — a live entity, not config, so it needs re-spawning after any restore that doesn't carry over existing entities. **2026-09-22 (round 3): attempted a re-mount fix for the follow bug — round 4's live playtest showed it didn't work,** the bar still never tracked a moving mob. **2026-09-22 (round 4): replaced vehicle-mounting with direct teleport-tracking** — `HEALTHBAR_ENTITY_determine_status` now runs `teleportto{location=@ParentLocation{y=<vertical_offset>}}` every 2 ticks instead of relying on a `mountme` passenger relationship at all; `vertical_offset` redefined as full height-above-feet (was a small offset above eye location) to match, GlitchReaver's override raised to 3.0 accordingly (still an estimate). **2026-09-22 (round 7): confirmed live — tracking works, but round 4's single GlitchReaver-only tall-mob override wasn't enough**, the bar sat inside the head/body on most other mobs too. Extended the override to all 10 roster mobs, grouped by vanilla base entity's eye height (`HEALTHBAR_ENTITY_offset_tier_tall/enderman/phantom` in `skills/healthbar.yml`) rather than listing each mob individually — still estimates, not measured against the live models. See docs/MODELS.md. |
| Dungeons (MythicDungeons) | Removed/pending config | **2026-09-22: GlitchDungeons removed entirely** (module deleted; live jar/data moved to `/opt/theglitch/removed-plugins-2026-09-22/`). Dungeons will be rebuilt on the already-installed MythicDungeons plugin instead, built via its own in-game GUI editor. Operator will provide a pre-built configuration later — until then, dungeons are unconfigured/unavailable. |
| Physical world content | Deferred | Hub facilities, dungeon shells, Red Zone POIs, and extraction structures are not reproducibly stored in Git. Container mechanic exists (2026-08-10); in-world placement of containers/spawn areas is operator work. |
| Death rules | Built (playtest pending) | GlitchDeathRules (2026-08-06): leggings+boots mercy keep on death in glitch_red + Red Zone entry invulnerability (30s, entry-point zones, cancel-on-action, glow). Coins is account-bound (`server/plugins/Coins/config.yml:85` `player-drop:false` `lose-on-death:false` `drop-on-death:false` — repo + live `coins reload` 2026-08-22). Built + deployed; loads clean on live (2026-08-21, re-verified 2026-08-23). Tuning pass (5.13.3) remains. |
| GlitchRaid | Built (playtest pending) | GlitchRaid plugin (2026-08-21, Folia-safe 2026-08-23 `FoliaScheduler.java:1`): `/raid start|end|status` + `/raidadmin`, party-of-4 with `FoliaScheduler.teleportEntity`, BossBar raid timer (1800s), loot/death per-player recap with end summary, auto-timeout, **real** `PlaceholderExpansion` `%glitchraid_*%` (`pom.xml:53` `https://repo.extendedclip.com/content/repositories/placeholderapi/` `d488a7d`). Deployed; loads clean on live (2026-08-21, re-verified 2026-08-23). |
| GlitchInsurance | Built (playtest pending) | GlitchInsurance plugin (2026-08-21): premium 100 shards/item (max 3), 300s claim window, 60s cooldown, Vault withdraw, per-player Base64 YAML with async atomic saves; `/insurance buy|list|claim` + `/insuranceadmin`. Needs `lib/VaultUnlocked.jar` at compile (auto-seeded by build-all.sh). Deployed; loads clean on live (2026-08-21). |
| GlitchEvents | Built (playtest pending) | GlitchEvents plugin (2026-08-22): auto-scheduler (random 20–45 min), supply drops (BARREL + loot near a random player, coordinate broadcast, timed removal), roaming bosses via MythicMobs console spawn, `/glitchevents start supply_drop|start roaming_boss|stop|reload|status`. Extraction windows stubbed/disabled by default. Deployed; loads clean on live (2026-08-22). |
| Mob loot | Implemented | **2026-09-22: GlitchLoot (the adaptive smart-loot plugin — dry-streak bonus, hourly power budget, anti-funnel cooldown) was removed entirely**, per operator decision to stop maintaining a separate loot layer. Mob drops now come solely from each mob's own MythicMobs `DropTables` entry (`server/plugins/MythicMobs/DropTables/`) with no adaptive/contextual adjustment on top. |
| Custom UI theming | Implemented | Arcane Ruins UI kit (2026-08-23): Oraxen font glyphs E040-E049 (`server/plugins/Nexo/glyphs/oraxen_glyphs/theglitch.yml` + `scripts/gen-ui-textures.py`), rank badge glyphs E050-E058; Wynncraft-style gear detail pages in `GearManager.buildItem`; glyph titles in Shops/Hideout + Stash/Classes configs. Bedrock sees plain text (glyphs paired with labels by design). **Chest-window art restyled 2026-09-22, then fixed same day (round 9)**: swapped the procedural void-purple `generic_54.png`-only override for a wood/parchment theme covering all 6 chest sizes (`generic_{9,18,27,36,45,54}.png`, both modern `gui/sprites/container/` and legacy `gui/container/` paths) — every custom chest GUI (Shop/Class/Class-Detail/Hideout/Stash/RedZone) is themed now, not just the 54-slot ones. Round 8 tried copying the operator-supplied "Medieval" GUI kit's PNGs verbatim; they turned out to be drawn centered in their 256x256 canvas rather than flush at vanilla's hardcoded (0,0) origin, which scrambled every item icon in-game (see round 9). Fixed by going back to procedural generation (`chest_window()` in `scripts/gen-ui-textures.py`, pinned to vanilla's real 176px/17px-header/18px-row coordinates) recolored to the kit's wood/parchment palette, with a proper 9-column slot grid. `assets/ui-kits/medieval/` still carries spare menu/jobs/profile/rewards template art + a button icon sheet + a 2-tone bitmap font, staged for the future Menu hub GUI (not yet wired — no plugin code or bitmap-font provider exists for them yet; note the same centering issue likely affects those template PNGs too, so they'd need the same fix before use). Glyphs/rank badges/player-inventory screen stay on the old void-purple palette, so there's a known cosmetic mismatch between the chest window and everything else until those get a matching pass. |
| Game loops | Partial | Insurance (5.9.4), dynamic events (5.9.6), smart loot (5.9.7), and **dynamic extraction (6.2)** now exist as plugins — playtests pending. Gear-score scatter weighting (Phase 6.3) still open (scatter now land-aware `[0..2000]²` `cd74932`). Residual Glitch loop fully wired; hideout progression built (2026-08-10). |
| Operations and launch | Planned | Backups, moderation/rollback, load testing, soft launch, and launch checklist remain. |

## Completed Foundation

- Phases 0-2: server setup, security, performance configuration, and idle baseline.
- Geyser/Floodgate configuration, pending live Bedrock verification.
- Zone rules, borders, and WorldGuard configuration scripts.
- Glitch Shards economy configuration and the full ten-mob MythicMobs roster with per-tier loot tables.
- GlitchStash extraction vault + GUI fixes (`6ef1425`) and merge overflow preservation (2026-08-10).
- GlitchClasses core plugin with complete abilities, ultimates, starter kit, and real reset costs (source 2026-08-10).
- GlitchDeathRules: mercy keep + Red Zone entry invulnerability (source 2026-08-06).
- GlitchHideout: 7 stations, workbench crafting, extended stash, armory (source 2026-08-10).
- Oraxen item definitions and resource-pack assets (20 items).
- GlitchItems + GlitchShops deployed and live-tested (2026-08-03): `/identify` and `/shop` buy/sell verified.
- GlitchHealthBar deployed and live-tested: floating HP bars above hostiles.
- Red Zone spawn areas (T1/T2/T3 distribution) + container system in source (2026-08-10).
- Cross-plugin bug audit (2026-08-10): compile-blocking Hideout type mismatch, stash merge data loss, shop sell-without-pay, hotbar overwrite, crash risks, upgrade-cost balance — all fixed in source.
- Efficiency roadmap (2026-08-21): root parent POM + `scripts/build-all.sh` reactor builds; hot-path config caching across all plugins; async atomic saves + ticker tuning; `plugins/GlitchCommon` shared library, `scripts/lib/{preflight,gamerules}.sh` shell dedupe, GitHub Actions CI (`.github/workflows/ci.yml`), `.editorconfig`, `config-version: 3` on all plugin configs.
- Phase 5.9 completed (2026-08-21/22): GlitchRaid + GlitchInsurance (`9f78d3a`) and GlitchEvents + GlitchLoot (`db0f69d`, lambda fix `425eb9f`). All built via the Maven reactor on the live host, deployed to `/opt/theglitch/server/plugins/`, and confirmed loading clean in `logs/latest.log` after restart.
- Follow-up polish 2026-08-23 (`aa4c0b3` Folia `FoliaScheduler.java:1` wrapper + real `RaidExpansion extends PlaceholderExpansion` + `ShopGUI.java:375` atomic economy, `d488a7d` add `pom.xml:53` PlaceholderAPI repo `https://repo.extendedclip.com/content/repositories/placeholderapi/`): reactor 14/14 `BUILD SUCCESS`, `PlaceholderAPI-2.12.3.jar` resolved, live restart 17:28 `Vault Enabled 2.20.2` `PlaceholderAPI 2.12.3` clean; Coins switched to account-bound `player-drop:false` `lose-on-death:false` `drop-on-death:false` and reloaded live (`coins reload` 10ms).
- Custom UI theming (2026-08-23): Arcane Ruins UI kit — 10 font glyphs + themed chest-window texture generated by `scripts/gen-ui-textures.py`, wired through Oraxen (`glyphs/theglitch.yml`, recursive pack sync in `scripts/setup-oraxen-items.sh`); gear tooltips rebuilt as Wynncraft-style detail pages (`GlitchUI.java`, `GearManager.buildItem`); rift/material lores restyled; Bazaar/Stash/Class/Hideout titles carry the rune glyph.
- **2026-09-01 — Dynamic extraction + HUD hardening:** `c1634b3` auto cycle (3 validated random arenas via `SpotPicker` + `DynamicExtractionManager` + `WaypointBridge`/`ExtractionMarkers`), `a0edffa` force-loaded marker chunks, `cd74932` scatter center `(1000,1000) r1000` for `[0..2000]²` land, `f312262` GlitchHUD icon sidebar (`NumberFormat.blank`, per-world layouts, `NOTCHED_10` bar, `StashCycleProbe`, TAB takeover + `negative_space.json` via `build-all.sh`), `c9a229e` ping/TPS fallback (`Player.getPing()`/`Bukkit.getTPS()`) + `ByteTag` PDC crash guard (`pdc.has` + try/catch) fixing container keys (`ContainerManager`/`HideoutManager`/`ShopManager`/`OraxenUtil`/`IdentifyManager`/`ExtractionVariantManager`), `9d8f05a` divider trimmed + 9-point tol2 flatness + 2-deep `isOccluding` reject + taller `y+4` region + ring `r*0.6` particles. Reactor `BUILD SUCCESS` and live restart verified `Cycle #1 3/3 at (670,299),(69,569),(299,942)`.
- **2026-09-02 — Economy + item balance pass (`4d8c554`+`b638e45`, docs/ITEM_BALANCE.md):** mob COINS retuned toward GAME_DESIGN §8 targets (T1 1-2, T2 2-6/3-8/5-10, T3 10-16, boss 40-80); gear sell now roll-based (`base + stars × bonus`); attribute pools widened (weapons: execute/frost-touch, armor: thorns/glitch-ward; Legendary = 2 distinct); archetype identity (staff flat ATTACK_DAMAGE per rarity, greatblade ATTACK_KNOCKBACK); consumables implemented (healing_potion/corrupted_heal effects, corrupted_heal 25% boss drop, Rift Attunement Pack = free identify any rarity, Void Infusion = held Epic+ gear boost+star reroll); Vault containers +5% legendary rift roll; craft EV fixes (base blade 3r+1c, targeted +1 aether); +2 alchemy items (Aether Tonic, Ward Salve — 20 items total); Oraxen repo configs migrated `displayname:` → `itemname:` to match live. Deployed via `scripts/deploy-balance-2026-09-02.sh` (build + config/droptable/Oraxen sync + `mm reload` + `oraxen reload all` + restart, `rift_vault=6` scatter confirmed in log).
- **2026-09-02 — Armor rework (f1da4d0):** armor upgrade levels +0..+5 at Workbench ANVIL slot40 or /armor upgrade, per-slot identity, cost shards 10/25/60/150/400 × [1,2,3,4,6] + materials, level excluded from sell, old gear +0, deployed/RCON verified, config v3 both sections live.
- **2026-09-14 — Pre-alpha hardening (`64ed02a`):** raid-buffer red-entry block (`RaidManager.denyRedEntryDuringBuffer` — world-change/join/portal/walk-in/party-pull, `glitchraid.admin` bypass); dialogs removed repo-wide (zero `DialogUI` callers, chest GUIs via `/shop /shopui /class /hideout /hideoutui /dungeon /stash /insurance`); `BazaarPanel`/`DungeonPanel` floating UX (yaw-aligned, per-row hitboxes, confirms fall back to chest GUI). Rank ladder live (Member 0 ← Wisp 20 ← Stalker 30 ← Sentinel 40; staff Helper 400 ← Moderator 500 ← Admin 1000 ← Owner 1100 + Dev branch; badges `E050-E058`; TAB sorting).
- **2026-09-14 — Custom models (`c32baa0`/`22cc690`/`4d73404`, docs/MODELS.md):** ModelEngine R4.1.0 + user clips converted pipeline-exact (78/78 @ 0.0000°) — warden `walk` (GentleSlowWalk 2.4s) with dual-path usm trigger + restored relative offsets + feet planted; winged wisp rig (full-rigid north bake). Runtime deploy (no restart), `3 models loaded`, pack merged, zero errors. Visual sign-off + wisp-scale decision pending.
- **2026-09-20 — Mythic marketplace add-ons purchased and deployed (jars live-only, gitignored):** MythicMobs upgraded free→**Premium** v5.13.1 (auto-activated, no manual key), MythicCrucible v5.13.1, MythicDungeons v2.1.0 (**needs ProtocolLib**, now installed from `dmulloy2/ProtocolLib` GitHub releases), MythicAchievements→**Premium** v1.2.0, MythicHUD v1.3.4. All installed to `/opt/theglitch/server/plugins/`; free `MythicMobs.jar` kept as `MythicMobs.jar.free.bak` for rollback. Fixed a permissions bug where `plugins/MythicMobs/` was owned `ubuntu:ubuntu` (from earlier config deploys) instead of `minecraft:minecraft`, which silently broke MythicMobs Premium's new pack-generator feature — `chown`'d back, generator now runs clean. One demo config each for Crucible (`GlitchTestBlade` test item, `server/plugins/MythicMobs/items/GlitchCrucibleItems.yml`) and Achievements (`WARDEN_SLAYER`, `server/plugins/MythicAchievements/Achievements/GlitchHunting.yml`), both verified loaded via `mm reload`/`mythicachievements reload` with no errors. **MythicHUD fails to enable** — `NullPointerException` in `DirectoryCopyFileVisitor.copy()` on the downloaded v1.3.4-all build; the vendor's dev-build changelog shows 26.2 support landing in later 1.3.4/1.3.5 builds than this one, so a newer premium dev-build (downloaded from the user's mythiccraft.io account) likely fixes it — blocked on the user grabbing that build. MythicDungeons has no repo config yet — it's built entirely via its own in-game GUI editor, not YAML.
- **2026-09-20 — Mythic add-ons configuration pass:** found and fixed a real conflict — MythicMobs Premium's built-in pack generator ships with `Deployment.Enabled: true` + `AutoSend.Enabled: true` (`config/config-generation.yml`), which would have independently uploaded and pushed its own resourcepack to players, racing Oraxen (this server's single pack authority that already merges ModelEngine/items/UI glyphs and sends via atlas.oraxen.com). Disabled both; `GenerateOnStartup` stays on so the merged Crucible/MythicHUD pack still builds to disk for future manual Oraxen-folding if needed (same pattern as ModelEngine). MythicDungeons `Spectator.UseLegacy` switched to `false` since `packetevents-2.13.0` is already installed (Geyser dependency) — modern spectator mode now available. Expanded `GlitchHunting.yml` from one demo achievement to one `KILL_MYTHIC_MOB` achievement per real mob (all 10 in `server/plugins/MythicMobs/Mobs/`, kill counts scaled by GAME_DESIGN tier: T1=50, T2=25, T3=5, T4 boss=1) plus a `GLITCH_BESTIARY` capstone requiring one of each. Found a MythicAchievements/MythicMobs quirk: an XP reward of exactly `500 EXPERIENCE` throws `Drop type not found` (WARN, non-fatal) while 30/60/100/250 all work — lowered the capstone reward to `300 EXPERIENCE` to avoid it rather than chase a closed-source parser bug. MythicCrucible intentionally left at one template item — a full item catalog is a game-design call for the operator, not something to invent unprompted; GlitchItems already owns the real item economy.
- **2026-09-20 — Migrated Oraxen → Nexo:** all 20 Arcane Ruins items + UI glyphs converted via Nexo's built-in Oraxen migration tool (`server/plugins/Nexo/`, replacing the removed `server/plugins/Oraxen/`); world backup taken first (`/opt/theglitch/backups/pre-nexo-migration-*.tar.gz`). Found and fixed real regressions the conversion alone didn't cover: (1) all 10 MythicMobs `DropTables/*.yml` used the `oraxen <id> <amt> <chance>` drop type, which only existed because Oraxen itself registered it with MythicMobs — swapped to `nexo` (Nexo's own MythicMobs compatibility layer). (2) Five separate hand-rolled Oraxen PDC-key scanners across `HideoutManager`, `ShopManager`, `ContainerManager`, `IdentifyManager`, `ExtractionVariantManager` (`NamespacedKey("oraxen","custom_item_id")`) — fixed to Nexo's actual key (`nexo:id`, confirmed live via `/nexo dumpitem`), since the shared `NexoUtil` bridge alone didn't cover these plugin-local duplicates. (3) `/o give <player> <id> <amount>` → `/nexo give <id> <amount> <player>` — Nexo's argument order is different, not just the command name (`StarterKit.java`, 7 recipe outputs in `GlitchHideout/config.yml`). (4) `GlitchCommon/OraxenUtil.java` rewritten as `NexoUtil.java` (same reflection-based, no-compile-dependency design). (5) `GlitchShops` had a *hard* compile-time Oraxen dependency (`io.th0rgal.oraxen.api.OraxenItems`, `system`-scope local jar) — swapped to a real Maven dependency on Nexo's public repo (`repo.nexomc.com`, `provided` scope, all transitives excluded — one pulled an unresolvable SNAPSHOT). `GlitchItems`' own Oraxen dependency was dead code (zero actual usage) — removed rather than swapped. All 13 non-deferred plugins rebuild clean (`GlitchDungeons` excluded, pre-existing/unrelated local-lib gap). Also fixed a pre-existing unrelated bug found while touching `scripts/backup-now.sh`: it referenced 6 host paths that no longer exist (`usernamecache.json`, `paper-global.yml`, `paper-world-defaults.yml`, `world-overrides`, `plugins/GlitchCommon`, `plugins/WorldEdit`) and was failing outright — removed the stale paths, added the new Mythic/Nexo jars to its licensed-jar backup list.
- **2026-09-21 — Three parallel Red Zone worlds + hub world-picker GUI:** added `glitch_red_eleria` ("Eleria") and `glitch_red_horizons` ("Horizons") as two new Red Zone maps alongside the original `glitch_red` ("The Glitch") — same full-loot PvPvE extraction mechanics, different terrain. World data copied into `hub/dimensions/minecraft/<name>/` and `mv import`'d live (no restart needed for import itself); gamerules/WorldGuard flags/gamemode/difficulty applied to match `glitch_red`. Found a real hazard during an automated RCON terrain scan: Horizons' spawn (0,0) had lava at y=80 — `mv setspawn` moved to `(30,81,0)` (confirmed dry land); Eleria's documented-but-unused entry-ring points are underwater (harmless today since the ring isn't the live entry path — see docs/ZONES.md). Replaced the hub `RedZoneGate` NPC's direct `mv tp glitch_red` action with a new `/redzone` command opening `RedZoneSelectGUI` (GlitchRaid, follows the `DungeonSelectGUI` chest-menu pattern) listing all three worlds by display name; picking one still dispatches `mv tp` so the existing auto-start/auto-join flow (`RaidListener.onWorldChange`) is unchanged. This FancyNpcs edit is a manual live-server step (NPC data isn't git-tracked) — done directly on the host, not via a script. Made the raid/extraction architecture genuinely multi-world instead of hardcoded to one: `GlitchRaid`'s `RaidManager` generalized `auto-start-world` (String) into `auto-start-worlds` (List) with `isRedWorld()`/`getAutoStartWorlds()`, added a `worldKey` field to `RaidSession` (eliminates several O(n) reverse-lookups against `globalSessions`), and fixed a real bug where `startRaid()` always anchored to the single configured world regardless of which red world the leader actually stood in — a raid started in Eleria would previously have touched `glitch_red`'s global session. `GlitchStash` now constructs one independent `(ExtractionMarkers, DynamicExtractionManager, AutoExtractScheduler)` instance-triple per configured `auto-extract.red-worlds` entry (each running its own 31-minute cycle and 3 dynamic extraction points) rather than one singleton for the whole plugin; fixed two real cross-world bugs found while doing this — `ExtractionVariantManager.setRuntimeZones()` replaced the *entire* cross-world zone map (now `setRuntimeZonesForWorld()`), and `ExtractionMarkers.clear()`'s backup sweep iterated *every* loaded world (now scoped to its own world) — both would have let one red world's cycle-end wipe another's still-active state. VelKoth arena ids are now world-namespaced (`extraction_dyn` / `extraction_dyn_eleria` / `extraction_dyn_horizons`, configurable) to prevent concurrent cycles from colliding. `GlitchHUD`'s "RED ZONE" title/scoreboard/below-name styling and `server/plugins/TAB/config.yml`'s scoreboard condition were generalized from a single hardcoded `"glitch_red"` literal to a config-driven set covering all three worlds. `MythicMobs`' `RedZone_RandomSpawns.yml` extended its `Worlds:` field to a comma-list across all three (loot itself needed no changes — MythicMobs DropTables are per-mob, not per-world). Seven other plugins (GlitchClasses, GlitchDeathRules, GlitchEvents, GlitchHealthBar, GlitchInsurance, GlitchLoot, GlitchItems×3) already read their world-gating from a config list, so those were simple appends. Every new config key has a legacy-singular fallback so a config with just `glitch_red` reproduces today's exact single-world behavior. **Live incident found on first deploy:** the very first extraction cycle on `glitch_red_eleria` froze the main thread for 30+ seconds (watchdog thread dumps) and the server crashed once (systemd auto-restarted it) — `SpotPicker.pickRandom()`'s `world.getChunkAt()` call synchronously *generates* any chunk that doesn't exist yet, which is harmless on `glitch_red` (fully Chunky-pre-generated) but not on the two freshly-imported maps, which only have scattered pre-generated coverage. Fixed by rejecting ungenerated chunks outright (`isChunkGenerated()` check) instead of ever forcing generation. Also wired up the `auto-extract.dynamic-overrides` config (documented but not yet read) so each red world's dynamic-extraction land box can be centered on its own actual terrain instead of the shared `(1000,1000)` default, which was mostly ocean on both new maps. After tuning, `glitch_red` and `glitch_red_horizons` reliably hit 3/3 validated points per cycle; `glitch_red_eleria` holds around 1/3 regardless of the box tried — genuinely sparse usable terrain in the explored area, not a bug, degrades gracefully rather than crashing.
- **2026-09-21 (later same day) — Fixed the real causes behind Eleria's low point count and both new worlds' missing mobs/chests:** the earlier "genuinely sparse terrain" conclusion was wrong. Root cause: `Eleria`/`Horizons` are external map imports that only ship the small footprint the original download covered — everything past that edge is ungenerated on disk, and `SpotPicker` (correctly, after the watchdog-freeze fix) refuses to ever force-generate chunks live, so most random attempts inside the configured land box landed on ungenerated chunks and were silently skipped. `scripts/setup-worlds.sh` had deliberately skipped Chunky pre-generation for imported red worlds on the assumption "they already have terrain" — true only inside the original download's footprint. Fix: ran a bounded Chunky pre-generation (square, centered on each world's `dynamic-overrides` box + margin: Eleria center (250,150) radius 450, Horizons center (150,100) radius 750) live via RCON — both completed in seconds (most of the requested area was already generated; only the gaps needed filling) — and folded the same bounded pregen into `setup-worlds.sh` (per-world center/radius table, marker-file-gated like the existing `glitch_red` pregen) so it's reproducible. This also explains the "no mobs / no chests" report: vanilla generation includes structures (mineshafts, ruins, villages) with vanilla loot chests, which the tiny original footprint had little room for — filling the gap with real generated terrain brings those back. Separately, and unrelated to terrain: `server/plugins/MythicMobs/randomspawns/RedZone_RandomSpawns.yml` is a server-side config file (not part of the Maven build/`build-all.sh` deploy path) — it had been edited in the repo during the original three-world work to list all three worlds, but that edit was never actually copied to the live server, so MythicMobs had silently kept spawning in `glitch_red` only. Fixed by deploying the file (`scp` + `sudo install`) and running `/mm reload`; confirmed via diff that the live file now matches the repo. The same live/repo drift affected 6 other plugins' world-gating lists (GlitchDeathRules, GlitchEvents, GlitchHealthBar, GlitchInsurance, GlitchLoot, GlitchItems×3, plus GlitchLoot's `loot.enabled-worlds`) — their config keys already existed on disk with only the old world list, so the jar-bundled-default fallback (which only helps when a key is *absent*) didn't apply; patched all 6 live files directly (backed up first, YAML-validated, applied on a restart). Block-break/block-place protection was independently re-verified already correctly denied (`WorldGuard __global__`) on all three red worlds, and `mob_griefing false` prevents mobs from altering blocks — no code change was needed there, it was already working (caught and immediately fixed a self-inflicted slip during that check: querying a WorldGuard flag via bare `rg flag <region> <flag>` with no value *clears* it, not reads it — briefly cleared `block-break` on all three worlds while checking, restored within the same minute).

Pre-generation alone did **not** fix Eleria's point count (still ~1/3), ruling out ungenerated chunks as the sole cause. Direct RCON relief measurements (9-point grid around a candidate spot) found the real cause: small-scale terrain unevenness of 3+ blocks almost everywhere sampled, exceeding `SpotPicker`'s hardcoded ±2 flatness tolerance. Made that tolerance configurable (`auto-extract.dynamic.flatness-tolerance`, default raised 2→4 for all worlds — strictly more permissive, safe for the already-3/3 maps) and made `min-separation` overridable per-world too (Eleria's validated points across cycles clustered within ~250 blocks of each other, making the shared 400-block separation impossible to satisfy for a 2nd/3rd point; set Eleria's to 100). Even with both live, Eleria still lands ~1-2/3 — its terrain is genuinely rolling almost everywhere sampled, not sparse or misconfigured. Recommended next step (not done here, needs a human's eyes on the map): op flies Eleria, finds a flat spot, creates a VelKoth arena there, adds it to `auto-extract.dynamic.fallback-arenas` — `SpotPicker.pick()` already fills in from that list whenever random validation comes up short.
- **2026-09-21 — Removed the Glitch Warden/Wisp custom ModelEngine rigs (operator request):** stripped the `model{}`/`defaultstate{}` skill lines from `server/plugins/MythicMobs/Mobs/GlitchWarden.yml` and `GlitchWisp.yml` — both mobs are back to their vanilla base-entity appearance (plain IRON_GOLEM / VEX), loot tables and combat behavior unchanged. Deleted the tracked blueprint files (`server/plugins/ModelEngine/blueprints/asset_c89517720db045279ea8fb46f3d93d5b.bbmodel`, `glitchwisp.bbmodel`) and the source packages (`GlitchWardenV2.zip`, `GlitchWisp.zip`, repo root) from git, removed the same files from the live host, ran `meg reload models` (0 loaded) so the generated resource pack no longer includes them, re-synced that pack to Nexo, and `mm reload`. docs/MODELS.md kept as pipeline reference for any future custom rig, updated to note the removal.
- **2026-09-21 (later same day) — Full-adopt custom ModelEngine rigs for the whole mob roster, sourced from 3 downloaded packs:** the operator dropped in `Beyond Mob Pack 1.0/` (5 models), `the_voids/` (3 models), and `EndMobsVol1 (2)/EndMobsVol1-Crucible.zip` (16 models by RopeFire — 8 base + 8 "Empowered" reskins), all already-valid ModelEngine packs (no conversion pipeline needed, unlike the raw myrlin bundles the removed Warden/Wisp rigs came from). Selected 10 models (2 Beyond, 3 the_voids, 5 EndMobsVol1) and applied them as **full adopts** — Type, AI, and complete multi-stage skill kit replaced, not just a cosmetic model bolt-on — to all 8 non-boss mobs (CorruptedCrawler←voids_ghost, GlitchWisp←end_wraith, GlitchStalker←ender_crawler, GlitchBrute←enderbruiser, GlitchPhantom←mage, GlitchSentinel←ender_watchman, GlitchSniper←voids_wizard, GlitchWarden←coffin_man) plus 2 brand-new mobs: **GlitchReaver** (endermauler's kit near-verbatim — boss bar, segmented tail render, teleport-maul, scream-of-ender AOE — a new T3.5 rare-standing-threat mini-boss, not a scheduled event like GlitchCore/TheGlitchKing) and **GlitchHarrower** (voids_mask's portal-ring ambush kit, new T3 elite). GlitchCore/TheGlitchKing stay vanilla (Warden/Ender Dragon) — no pack had a matching boss-scale rig, confirmed with the operator before starting. Two deliberate exceptions to full-adopt: GlitchPhantom kept its native PHANTOM flight AI (only the mage model + burst-attack skill layered on top, since a grounded caster model would lose the mob's flying identity), and GlitchWarden kept its original vortex-pull + damage field as a bonus layered on coffin_man's kit (the mob's namesake mechanic). Health/Damage rescaled from each source mob's own numbers to fit our existing GAME_DESIGN.md tier bands; every mob's `Drops:`/`ScoreboardTags:` stayed on our own DropTable/resonance system, discarding the source packs' own item drops entirely. Added `server/plugins/MythicMobs/Skills/<Name>Skills.yml` (previously-empty folder) holding the ported, renamed multi-stage skill blocks per mob, plus a shared `GlitchSharedSkills.yml` + `GlitchVoidwake.yml` (an invincible 1-tick VFX helper mob for the EndMobsVol1-derived teleport effects). Two new DropTables (`GlitchReaverLoot.yml` between T3 and T4, `GlitchHarrowerLoot.yml` matching the T3 pattern), a new `RZ_T35_Reaver` randomspawns group plus GlitchHarrower folded into the existing T3 group, and 2 new MythicAchievements entries (`REAVER_HUNTER`, `HARROWER_HUNTER`) with the `GLITCH_BESTIARY` capstone extended to 12 mobs. EndMobsVol1's 5 picks use custom `tugkandeman.*` sound events — merged their `sounds.json` + 36 `.ogg` files into `server/plugins/Nexo/pack/assets/minecraft/` (all namespaced, zero collision risk); deliberately did **not** merge that pack's bundled vanilla-enderman texture overrides or custom boss-bar texture (would reskin every vanilla enderman server-wide, and needs client-side verification we haven't done — GlitchReaver's boss bar uses the vanilla PURPLE/SEGMENTED_10 style instead). Verified every `skill{s=...}`/`randomskill`/`oT=`/`CastInstead` reference resolves to a real skill definition and every `model{mid=...}`/`ModelPart{mid=...}` reference matches a deployed blueprint file (scripted cross-check, zero mismatches) before deploying — this pack also exercises several MythicMobs mechanics (`ModelPart` hitboxes, `totem`, `segment`/`renderinit`, `aura`, `lockmodel`) our original 10 mobs never used, so the live `mm reload` log needs a close look for "unknown mechanic" warnings.

- **2026-09-21 (later same day) — Custom loot-crate furniture models + key texture pass:** the operator dropped in two more downloaded packs — `Loot Models.zip` (6 ItemsAdder "elemental crate" models: fire/water/earth/air/light/darkness) and `key1.zip` (8 key skins × 2 variants, already shipped in both ItemsAdder and Nexo config formats). Applied 3 of the 6 crate models as Nexo **furniture** items (`loot_crates_fire`→Debris Pile, `loot_crates_earth`→Loot Cache, `loot_crates_darkness`→Rift Vault; Vault keeps its vanilla BLUE_SHULKER_BOX — no matching model picked) and 4 key textures (wood2→Cache Key, golden2→Vault Key, mythic2→Rift Key, fire2→Fast Extract Key — texture-only, same item ids/materials). The crate work surfaced a real architectural conflict, resolved with the operator before implementing (see chat, not a docs section): our loot containers are placed **at scale** by `ScatterManager` (~400 per cycle, every ~30m, RED World only) as real blocks, with container identity/regen state stored in each block's own `PersistentDataContainer` — but Nexo furniture is entity-based (no tile entity, can't hold block PDC), and Nexo's noteblock-based custom-block alternative also isn't a tile entity. Fix (operator chose "full refactor"): `ContainerManager` now tracks **all** container state (both furniture and legacy-block types) in a new location-keyed store (`plugins/GlitchItems/data/containers.json`, same Gson atomic-write pattern as `ScatterManager`'s own `scattered.json`) instead of block PDC. `ScatterManager.placeBlock()`/`clearBlock()` now call `NexoFurniture.place()`/`.remove()` for furniture types and unchanged `block.setType()` for legacy types, transparently via `ContainerManager.mark(Location,...)`/`clear(Location)`. New `ContainerFurnitureListener` handles Nexo's `NexoFurnitureInteractEvent` (denies Nexo's own storage GUI, runs our loot-roll logic instead) alongside the existing block-based `ContainerListener`. Added a real (`provided`-scope) Nexo Maven dependency to `GlitchItems` (was reflection-only via `NexoUtil` before, matching `GlitchShops`' existing pattern) to get compile-time access to `NexoFurniture`/`NexoBlocks`/`NexoFurnitureInteractEvent` — confirmed their exact API via `javap` against the live `nexo-1.25.1.jar` (web docs for Nexo's furniture/custom-block YAML schema were incomplete/inconsistent across fetches, so the bundled `items/nexo_defaults/nexo_furniture.yml` example and jar reflection were used as ground truth instead of trusting them). Compiles clean (`mvn package` both locally and reactor-wide). Live in-game verification (crate visuals actually render, right-click loot roll fires, furniture placement via `/glitchcontainers set` and the next scatter cycle) is pending — needs a relog to fetch the pack and a scatter cycle or manual `/glitchcontainers set`/`scatter` to observe.
- **2026-09-21 (later same day) — Per-world scatter bounds + mob density retune:** the previous same-day pregen fix used the shared global square (center 1000,1000, radius 1000) for Eleria/Horizons too, which was wrong — their spawn points sit near world origin ((0,0) and (30,0)), not (1000,1000), meaning that pregen (and every past `ScatterManager` cycle) only ever covered a fraction of each imported map, offset in the wrong direction. Operator confirmed the real map sizes (Horizons 3200x1400, Eleria 3000x1500, both centered on world origin) and cancelled/redid the pregen as a rectangle via Chunky's `corners` command centered correctly. Root-caused and fixed the same shared-coordinate-space assumption in `ScatterManager` itself: it previously scattered loot using one global square border and only ever refreshed a single arbitrarily-picked world per cycle (`pickWorld()`), silently leaving the other two red worlds' loot stale. Added a `WorldBounds` record + `scatter.worlds.<name>.{min-x,max-x,min-z,max-z}` config (falls back to the legacy global square for any unlisted world), and `scatterNow()` now loops every enabled+loaded world each cycle with its own bounds instead of picking one. Also retuned `RedZone_RandomSpawns.yml`'s T1-T3 mob density (Chance/Cooldown) up moderately — the 2026-08-24 "too dense" fix had overshot into "rarely see a mob" territory (operator report), especially noticeable since `SpawnRadiusPerPlayer` is only 64 blocks so a low per-check Chance compounds into long dry stretches while exploring. Left the T3.5 mini-boss and T4 boss entries untouched (should stay rare by design).
- **2026-09-21 (later same day) — Vault crate model + fixed the scatter watchdog-crash root cause:** Vault now also uses a crate furniture model (`loot_crates_water`, blue theme matching its old BLUE_SHULKER_BOX) — all 4 containers are furniture-backed now, none use a vanilla block. Fixed the actual bug behind the same-day scatter crash: `ScatterManager.placeNew()` called `world.getChunkAt()` on unloaded chunks, which force-generates them synchronously on the main thread if they don't exist on disk — fine on `glitch_red` (fully pre-generated) but a watchdog-timeout risk on `glitch_red_eleria`/`glitch_red_horizons`, which only had a small extraction-box-sized area pre-generated, not the full scatter border. Two-part fix: (1) code guard — `ScatterManager` now checks `world.isChunkGenerated(cx,cz)` first and skips (never generates) ungenerated chunks, same pattern already used by GlitchStash's `SpotPicker`; (2) actually eliminated the gap — widened `scripts/setup-worlds.sh`'s imported-world pre-generation from the old small extraction-box-centered area (Eleria center (250,150) r=450, Horizons center (150,100) r=750) to match `ScatterManager`'s full scatter border exactly (center (1000,1000) r=1000, both worlds — the same area `glitch_red` itself got), and ran that wider Chunky pass live on both worlds (Chunky only fills gaps, doesn't redo existing terrain). The operator's framing was right: the fix is "the server should always have those chunks generated," not just "don't crash when they aren't" — the code guard is a safety net for edge cases (future border changes, deleted regions), not the primary fix.
- **2026-09-22 — Removed TheGlitchKing, capped Red Zone mob density, and fixed loot never appearing after a restart:** operator reported joining Eleria to find 50-100 mobs piled at spawn, a suspicion that the dragon boss "creates multiple mobs when it dies," and no loot anywhere in any of the 3 red worlds. All three traced to real, separate bugs. (1) `server/plugins/MythicMobs/config/config-spawning.yml`'s `RandomSpawning.SpawningLimit`/`LocalSpawningLimit` were both `-1` (unlimited) — the per-entry `Chance`/`Cooldown` tuning in `RedZone_RandomSpawns.yml` throttles how often a spawn *attempt* fires, but nothing capped how many mobs could accumulate in one place, and mobs within ~32 blocks of an idling player never naturally despawn; now `SpawningLimit: 150` / `LocalSpawningLimit: 25` (file newly tracked in-repo, previously live-only). (2) It wasn't actually an on-death spawn: `TheGlitchKing.yml`'s Phase-1/Phase-3 `summon{m=GlitchStalker}` skills used `~onTimer:` triggers with no health-range condition, so both fired continuously the entire time the dragon was alive and un-killed, compounding with the density-cap bug above. Removed `TheGlitchKing` entirely per operator request (mob file, drop table, `RZ_T4_Bosses` randomspawns entry, `KING_SLAYER` achievement, `GLITCH_BESTIARY` capstone criterion) — nothing else referenced it. `GlitchCore` was pulled from the same open-world random-spawn entry for the same density reasoning, but its mob file/drop table/achievement were deliberately kept: `plugins/GlitchDungeons/src/main/resources/config.yml` spawns it by type name as the scripted boss for 4 dungeon tiers, and deleting it would have silently broken that unrelated feature. (3) Loot: `ScatterManager` only ever ran on a 31-minute timer or GlitchStash's `AutoExtractCycleEndEvent` (t0+30m+5s) — there was never an initial scatter on plugin startup, so any restart (the server has been crash-restarting periodically, restart counter 18) left the map with whatever partial container set survived (105, found live) for up to half an hour, which is what the operator saw as "no loot in any world." Added a one-time delayed `scatterNow()` call in `ScatterManager`'s constructor (200-tick/10s delay to let worlds finish loading) so loot is always present shortly after boot. Also found and fixed, while pulling a fresh watchdog thread dump to investigate: `FoliaScheduler.isFolia()` called `Class.forName("io.papermc.paper.threadedregions.RegionizedServer")` — an always-throws reflective classloader walk on this non-Folia server — on *every* invocation, and `ScatterManager.placeNew()` calls it once per placement attempt (935 attempts in the captured dump); this was pinned as the actual top-of-stack cause of a fresh 55-second main-thread stall/crash during scatter (a regression the 2026-09-21 `isChunkGenerated`→`regionFileExists` fix didn't touch, since it's a different call site). Now cached once at class-init like the existing `HAS_PAPER_SCHEDULER` check. Deployed and restarted; live verification confirmed: scatter completed in 9s (down from 28-55s pre-`isFolia` fix) with zero watchdog stalls, 1248 containers tracked across all 3 worlds, MythicMobs/MythicAchievements loaded with no errors.
- **2026-09-22 (later same day) — Removed GlitchCore entirely, including from GlitchDungeons:** operator asked to finish the boss removal started earlier the same day by also pulling GlitchCore out of GlitchDungeons, and said GlitchDungeons itself will be replaced by the MythicDungeons plugin (already installed, currently unused) later. Deleted `GlitchCore.yml`/`GlitchCoreLoot.yml`, removed the `CORE_BREAKER` achievement and the `GLITCH_BESTIARY` `CORE` criterion (bestiary now requires 11 mobs, not 12). `plugins/GlitchDungeons/src/main/resources/config.yml`'s 4 dungeon tiers and `WaveManager.java`'s fallback default both spawned their wave-boss as `type: GlitchCore` (counts 1/1/2/3 across the 4 tiers, unchanged) — swapped to `GlitchReaver` as an interim substitute so the still-live GlitchDungeons plugin doesn't error trying to spawn a now-nonexistent mob type. This is explicitly a stopgap, not a rebalance — dungeon tier 3 is still named "Glitch Core" despite no longer fighting one, and boss counts/HP weren't retuned for the swap (3x GlitchReaver at 350 HP each for tier 4's "The Abyss" may be harder or easier than 3x old GlitchCore at 600 HP — untested). Proper rebalancing deferred to the planned MythicDungeons migration.
- **2026-09-22 (later still) — Removed GlitchDungeons, GlitchHUD, GlitchHealthBar, and GlitchLoot entirely, in favor of their Mythic-plugin equivalents:** operator asked (in one message) to (1) delete GlitchDungeons outright rather than keep patching it as a MythicDungeons stopgap, (2) swap GlitchHUD for a newer MythicHUD build the operator had downloaded, (3) swap GlitchHealthBar for MythicMobs' own native health-bar hologram since dungeons/PVE will only ever have MythicMobs mobs (not vanilla ones), and (4) drop GlitchLoot in favor of plain MythicMobs DropTables to cut a plugin to maintain. Before removing anything, checked every other plugin for real (non-comment) Java references to these 4 — found none; all cross-plugin mentions (`GlitchRaid`, `GlitchStash`'s `AutoExtractScheduler`/`AutoExtractCycleEndEvent`) were either javadoc comments or soft reflection probes (`pluginManager.getPlugin("GlitchLoot")`) that already fall through cleanly to the real live path (GlitchItems' `ContainerManager`) when absent, so removal was safe with zero code changes elsewhere.
  - **GlitchDungeons:** module deleted from `pom.xml`/`build-all.sh` (including the `GlitchDungeons` lib-seeding case block and the now-dead `ALL_WITH_DUNGEONS` array, replaced by validating against `TRACK1_ORDER`), source directory removed. Live jar + data folder moved (not deleted) to `/opt/theglitch/removed-plugins-2026-09-22/` for recovery. Dungeons are unconfigured until the operator provides a MythicDungeons config later.
  - **GlitchHUD → MythicHUD:** module deleted; `build-all.sh`'s GlitchHUD-specific "TAB takeover + Nexo negative_space sync" extras block removed (not re-wired to MythicHUD — that's the operator's call once they configure it). Installed the operator's newer `MythicHUD-1.3.5-SNAPSHOT-all.jar` (the live 1.3.4 build had thrown a `NullPointerException` on every enable since 2026-09-20, see the 2026-09-20 entry above) as `MythicHUD.jar`, keeping the broken 1.3.4 jar as `MythicHUD.jar.1.3.4-broken.bak`. Not configured yet — pending the operator's pre-built config.
  - **GlitchHealthBar → MythicMobs native `HealthBar`:** module deleted. Researched MythicMobs 5.13's actual health-display mechanisms first (the wiki's `Options.ShowHealth` — which all 10 mobs already had set — turned out to be an unrelated feature: it broadcasts a chat message on damage, not a hologram; the real floating-bar mechanism is a top-level per-mob `HealthBar: {Enabled: true, Offset: <n>}` field, styled globally via the already-present `server/plugins/MythicMobs/config/config-mobs.yml` `Holograms.HealthBar` block). Added `HealthBar: {Enabled: true, Offset: <n>}` to all 10 roster mob files (`GlitchVoidwake`, the invisible VFX helper mob, excluded) and removed the now-understood-to-be-irrelevant `ShowHealth: true` from all 10 — it would otherwise have chat-spammed every hit. Offsets are a first guess per base entity type (Enderman 3.2, Zombie/Vindicator/Skeleton 2.3, Phantom 1.0, scaled-down Corrupted Crawler 2.0) since the actual ModelEngine rig heights aren't verifiable without a client; flagged in docs/MODELS.md as needing visual tuning.
  - **GlitchLoot:** module deleted. Mob loot now comes solely from each mob's own MythicMobs `DropTables` entry with no adaptive dry-streak/power-budget/anti-funnel layer on top — an intentional simplification, not an oversight.
  - All 4 removals verified with a full local `mvn compile` across the remaining 10-module reactor (clean) before pushing. Live: old jars/folders relocated (not deleted) off `/opt/theglitch/server/plugins/`, new MythicHUD jar installed, deployed via `git pull` + `sudo ./scripts/build-all.sh` — 9/9 plugins built and deployed clean, `HealthBar` fields loaded with no MythicMobs config errors (11/11 mobs), server active with 0 players.
- **2026-09-22 (later still) — Actually fixed MythicHUD's `NullPointerException` (root-caused, not just a newer-build swap):** the v1.3.5-SNAPSHOT jar above still threw the exact same crash as v1.3.4 on enable — same stack trace, same `DirectoryCopyFileVisitor.copy()` call. Disassembled the class with `javap` (no decompiler needed — the failing line is just `getClass().getResource("").toURI()`) and found the real cause: `Class.getResource("")` — used to locate the plugin's own package/bundled-resource directory — depends on the jar having an explicit zip *directory entry* for that package, and this build's packaging pipeline had stripped **every single directory entry** from the jar (confirmed: `unzip -l` showed 0 directory entries across 2211 files, same in the old 1.3.4 jar too, so this isn't new to 1.3.5). Fix: extracted the full jar and re-packaged it with the plain JDK `jar` tool (`jar cf`), which adds directory entries by default — purely a zip-container repair. Verified before deploying: (1) every extracted class/resource file's MD5 matches the original byte-for-byte (2211/2211), (2) the jar has no `.SF`/`.RSA` signature files to break, (3) a standalone `URLClassLoader` test confirmed `getResource("")` returns `null` against the original jar and a valid `jar:file:...` URL against the repackaged one. Deployed as `MythicHUD.jar` (both broken originals kept as `.bak`); **MythicHUD now enables successfully** — `Mythic support detected`, `Floodgate Geyser support detected`, `PlaceholderAPI parser registered`, resource pack generation runs — confirmed live via a fresh restart with 0 players, only remaining ERROR in the whole boot log is the long-standing unrelated Essentials/MC-26.x incompatibility. Still no sidebar/HUD content configured — that's a separate step, pending the operator's own config.
- **2026-09-22 (round 3) — Scoreboard polish, health bar bug fixes, party HUD via Parties plugin:** Three follow-up rounds of live-screenshot-driven fixes after the MythicHUD adoption above. (1) TAB scoreboard: added the title-rune glyph (``) flanking every title and a real 8-star Residual Glitch meter (`conditions:` block, `%glitchitems_stacks%>=N` per star, gold vs dim glyph) replacing plain "N/max" text — matches the deleted GlitchHUD's style more closely (`server/plugins/TAB/config.yml`). (2) Removed two redundant top-of-screen bars: GlitchItems' own Residual Glitch `BossBar` (new `residual-glitch.show-bossbar: false` config toggle, `ResidualGlitchManager.java`) and GlitchReaver's native MythicMobs `BossBar` — both duplicated info already on the scoreboard/in-world health bar. (3) Fixed two littleroom-healthbar bugs: the bar didn't follow a moving mob (added a self-healing periodic re-mount) and rendered mid-body on tall custom-modeled mobs (added a per-mob-type vertical offset override, see the Mob health bars row above). (4) **Party HUD:** root-caused as an architecture problem, not a placeholder-syntax problem — MythicHUD's party template needs a supported party plugin (Parties/MMOCore/Mythic Dungeons) to resolve "who is member N" at all, which no amount of custom-placeholder tuning could work around (two earlier attempts, both custom `%glitchraid_member_N_*%` and a ParseOther chain, rendered as identical raw unparsed text with no server-side error). Installed the Parties plugin (`com.alessiodp.parties`, downloaded+verified via SHA-1 from AlessioDP's official Maven/file repos — the raw Maven module jar turned out to be a compile-only artifact missing its bundled ADPCore dependency and failed to load; the properly shaded distributable came from AlessioDP's own file server instead) and added `PartiesBridge.java` (GlitchRaid) to mirror GlitchRaid party membership into a same-shape Parties party on every create/invite-accept/kick/leave; `partyhud-p.yml`/`partyhud-p-listeners.yml` now read `%parties_list_rank_leader_1%` / `%parties_list_rank_member_1-3%` (leader always Parties rank "leader", others always default "member" rank) instead of the old custom `%glitchraid_member_N_*%` placeholders (removed from `RaidExpansion.java` as dead code). `parties.user.*` granted to the default LuckPerms group (defaults to op otherwise) — grant unverified over RCON since LuckPerms' Adventure-based command feedback doesn't route through RCON's response capture; worth a manual `/lp group default info` check in-game. None of items 1-4 has been visually confirmed by a live player yet (0 players online throughout).
- **2026-09-22 (round 4) — First live playtest of round 3's fixes, with real fixes this time:** operator got in-game for the first time since the GlitchHUD→MythicHUD switch. (1) **Health bar still didn't follow moving mobs** — round 3's periodic re-mount safety net didn't help, so the vehicle-mount approach (`mountme`) was dropped entirely. `HEALTHBAR_ENTITY_determine_status` now runs `teleportto{location=@ParentLocation{y=<vertical_offset>}}` every 2 ticks, reading the mob's live location directly instead of depending on Bukkit passenger mechanics — `vertical_offset` redefined from "small offset above eye location" to "full height above the mob's feet" to match the new anchor (default 0.25→2.0, GlitchReaver's override 1.1→3.0, both still estimates). (2) **Party HUD "kind of works"** — confirmed live once the operator ran `/party create`, but that's Parties' own native command, not GlitchRaid's (`/raid invite`); `parties.user.*` had been granted to the default rank purely so the HUD could resolve placeholders, which was unnecessary (placeholder resolution doesn't check command permissions) and let players create shadow parties GlitchRaid knows nothing about — revoked. Added a hub-world hide condition (party HUD is raid-only, not shown in the hub) and per-slot hide-if-empty conditions (`%parties_members_total%`) so a solo or small party doesn't render blank placeholder slots for missing members. (3) Fixed three `mmohud` HUD issues found in a zoomed screenshot: removed the "experience" element (class level/XP bar) entirely — its background graphic overlapped the residual/health row above it and its raw `%fill-value%/%fill-max%` text read as a stray floating number; bumped the skill bar's vertical offset (65→85) to stop it overlapping the resource-bar cluster above it. A separate raw-placeholder-text sighting on the main health bar text (`%player_health_rounded%`-style) could not be root-caused this round — nothing in that element's config changed and no other report of it recurring since; flagged as possibly a transient post-join HUD-refresh artifact rather than a persistent break, needs another look. All changes are config/asset-only (`mm reload` + `mh reload`, no restart) except the LuckPerms permission change. Deployed with 0 players online; none of this has been re-confirmed live yet.
- **2026-09-22 (round 5) — Removed the imported Volya HUD Bundle entirely, kept the TAB scoreboard:** after two full rounds of live-playtest fixes on the skill bar/resource bar/party HUD (title-rune scoreboard aside), operator decided it wasn't worth the continued maintenance burden and asked to remove all of it, keeping just the TAB scoreboard. Deleted the HUD content (`hud_assets/hud/{mmohud,partyhud-p,skillbar}.yml`, matching `layouts/`/`listeners/` files, the `mmohud-font`/`volya` font definitions, and their ~100 texture/font asset files under `source-pack/`) and set `MythicHUD`'s `layout: default: []` so nothing renders; the plugin itself stays installed for a possible future config. Also removed the **Parties** plugin and GlitchRaid's `PartiesBridge.java` (installed in round 3 solely to support the now-removed party HUD) — `PartyManager.java` reverted to its pre-bridge form, `pom.xml`/`plugin.yml` dependencies removed. Confirmed live: `GlitchRaid` and `MythicHUD` both enable clean with zero mentions of Parties anywhere in the boot log. Separately, the operator reported mobs "walking so fast now" after the health-bar teleport-tracking fix landed, suspecting a global speed/tick-rate change — investigated and found no evidence of one: `GlitchReaver`'s own `MovementSpeed: 0.4` is unchanged, no global MythicMobs speed multiplier exists in this config, and live TPS reads a healthy 20.0/19.9 (5s/15m). No code-level cause found; most likely explanation is perceptual — the health bar now visibly moves with the mob instead of sitting static, making its motion read as faster than before. Flagged as unresolved/needs a repro (which mob, which world) if it keeps coming up.
- **2026-09-22 (round 6) — Found the real cause of the Eleria mob-speed report:** operator was certain it was real ("way too fast", specifically on a GlitchReaver in Eleria) and right — round 5's TPS/perception theory was wrong. Every one of GlitchReaver's 6 combat skills (`RightSlash`, `LeftSlash`, `DashBiteStartup`/`DashBite`, `Scream`, `TeleportMaul`, `CloseSlashes`) freezes movement during its wind-up (`setspeed{speed=0}`) then "restores" it afterward with a hardcoded `setspeed{speed=1.5;type=WALKING}` — but GlitchReaver.yml's actual `MovementSpeed` is `0.4`. The 1.5 is a leftover from the source EndMobsVol1 pack's own base speed, never updated when we rescaled the mob for our tier bands. Since nothing else ever resets it, a Reaver's speed permanently jumps to 1.5 (3.75x normal) after its very first attack and stays there — completely unrelated to the same-day health-bar tracking change, which never touches the mob at all. Fixed by changing all 6 hardcoded restores to `0.4` (`server/plugins/MythicMobs/Skills/GlitchReaverSkills.yml`). Deployed via `mm reload`, no restart needed; a Reaver already stuck at 1.5 from before this fix will self-correct next time it uses an attack skill (or on despawn/respawn).
- **2026-09-22 (round 7) — Restored vanilla hunger/health HUD, actually deployed the residual boss-bar toggle, extended health-bar sizing to all mobs:** three bugs from a live screenshot. (1) **Vanilla hunger/health bar was invisible** — round 5's HUD removal deleted the *config* for the imported bars but left ~66 texture files under `MythicHUD/source-pack/assets/minecraft/textures/gui/` that the Volya bundle used to *blank out* the real vanilla heart/food/air/armor/XP icons (confirmed: 9x9 fully-transparent PNGs) so its own custom bars could stand in for them — with those bars gone and the blanking textures still merged into the resourcepack, nothing showed at all. Deleted the whole override folder and regenerated the pack (`mh reload` + `nexo reload`); vanilla HUD renders normally again. (2) **The Residual Glitch boss bar was still showing** despite round 3's `show-bossbar: false` fix — root cause: that change only ever touched the Maven-bundled *default* config template (`plugins/GlitchItems/src/main/resources/config.yml`), never the actual live `server/plugins/GlitchItems/config.yml` on the host, which Bukkit doesn't overwrite once a config file already exists. Patched the live file directly and reloaded via `/glitchitems reload` (no restart needed). (3) **Health bar still mid-body on most mobs** — round 4's tall-mob override only covered GlitchReaver; all 10 roster mobs wear custom Enderman-scale models over a mix of short (ZOMBIE/VINDICATOR/SKELETON), already-tall (ENDERMAN), and flying (PHANTOM) vanilla bases, so one override wasn't close to enough. Extended `HEALTHBAR_ENTITY_tall_mob_offsets` into three tier skills grouped by base-entity eye height instead of per-mob entries. All three fixes are config/asset-only (no restart); the boss-bar and mid-body sizing fixes are direct fixes for confirmed-live bugs, not guesses — the vanilla-HUD and sizing fixes still need a live look to confirm.
- **2026-09-22 (round 8) — Integrated the operator-supplied "GUI pack" (ItemsAdder-format, wood/parchment "Medieval" chest windows) as the new chest-GUI skin, replacing the old procedural void-purple `generic_54.png`-only override:** the zip (`GUI_pack.zip`, root-dropped, gitignored) had two parts: an `ItemsAdder/` folder using IA's `font_images` mechanic (a background PNG rendered as a custom-font glyph positioned in the inventory *title* — Nexo's glyph system, already used for the E040-E058 icon glyphs in `theglitch.yml`, is the equivalent primitive) and a flat `Medieval/` folder with the same art pre-rendered as plain `generic_{9,18,27,36,45,54}.png` vanilla-container overrides (256x256 each) plus `menu_template.png`/`jobs_template.png`/`profile_template.png`/`rewards_template.png` (same 256x256 shape, distinct banner art), a `buttons.png` icon sheet, and a two-tone bitmap font (`typography_title.png`/`typography_button.png`). Went with the plain-override route since it needs zero plugin code and reskins every real chest GUI in one shot: tracked the 13 useful PNGs at `assets/ui-kits/medieval/` (repo source, `.psd` and ItemsAdder-specific files left out), rewrote `scripts/gen-ui-textures.py`'s chest-window step from a procedural single-54-slot generator into a straight copy across **all 6** chest sizes — previously only 54-slot GUIs had themed art; Class Detail (45 slots) and Select Red Zone (27 slots) were silently falling back to vanilla stone, now fixed too. Glyphs/rank badges/player-inventory-screen stay on the old procedural void-purple palette (no replacement art was provided for those), so there's a known cosmetic mismatch between the new wood-toned chest window and everything else — flagged, not fixed, since fixing it means either re-theming the glyphs or finding/generating more matching art. The four template PNGs + buttons + bitmap font are staged at `assets/ui-kits/medieval/` only (not copied into the live pack) for the still-unbuilt Menu hub GUI (`menu_template.png` is purpose-fit: banner + 3 icon-card slots over a player-inventory grid) — wiring them needs the actual GUI Java class (deferred by the operator) and, if the IA-style glyph-title approach is wanted instead of another plain container override, a new Nexo glyph entry sized to the image. `nexo reload`-only change (asset swap, no restart).
- **2026-09-22 (round 9) — Fixed round 8's chest GUIs being visibly scrambled in-game (operator screenshot: item icons floating disconnected from the drawn cell grid, panel looking squeezed):** root-caused by measuring the Medieval kit's `generic_54.png` pixel-by-pixel — the artist drew their panel **centered** in the 256x256 canvas (actual content spans x=29..226, y=16..252), but vanilla's container-texture blit is hardcoded to start at pixel (0,0) with a fixed 176px width — it does not detect or center content. With ~29px of transparent margin where vanilla expected the panel's left edge, every item icon rendered at vanilla's real (unaffected) slot coordinates while the background art was shifted right underneath it, producing exactly the scattered look in the screenshots. This makes the raw kit PNGs **not usable as direct generic_N.png overrides**, full stop — no amount of `nexo reload`-ing fixes a source-art problem. Reverted `scripts/gen-ui-textures.py`'s chest window back to procedural generation (`chest_window()`, replacing round 8's `copy_chest_windows()`), which is pinned to vanilla's exact coordinates by construction (same 176px/17px-header/18px-row math as the original Arcane Ruins generator, proven correct across rounds 1-8) — but recolored it to the kit's wood/parchment palette (sampled with a small script: border `#613325`, parchment `#DEC5A0`/`#BC9E7A`, gold accent `#FBB954`) instead of copying the kit's art, and added a proper 9-column x N-row grid (18px pitch, matching vanilla's real slot pitch) so it still reads as a themed medieval chest, not a flat gradient. `assets/ui-kits/medieval/generic_*.png` are kept in the repo for reference but are no longer copied into the pack; the menu/jobs/profile/rewards template art is unaffected (was already staged-only, never live). Verified the regenerated `generic_54.png`/`generic_27.png` visually (flush at (0,0), correct 176x{17+rows*18} proportions, 9-column grid) before redeploying — deployed the same way as round 8 (scp to a temp path on the host, `sudo cp` into `/opt/theglitch/server/plugins/Nexo/pack/.../gui/{container,sprites/container}/`, `nexo reload`), 0 players online. Confirmed fixed via an operator screenshot: item icons now sit correctly inside the drawn grid. **Same-day follow-up:** the player's own inventory + hotbar (always shown below the container in any chest-style GUI, at a fixed vanilla offset right after the container's N rows — not a separate texture file) had been left fully transparent by `chest_window()` since round 8, which rendered in-game as a jarring purple/black checkerboard clashing against the new wood/parchment top (operator screenshot of the Stash GUI). Extended `chest_window()` to also paint that fixed-offset region — 4px gap, 3 rows, 4px gap, 1 hotbar row — with a darker reddish-brown variant of the same palette, matching the kit's own light-top/dark-bottom two-tone split (this time at the correct, non-centered coordinates). Deployed the same way, 0 players online.
- **2026-09-22 (round 9, second follow-up) — Fixed the player-inventory section rendering a visibly different WIDTH/shape than the container above it, and re-themed the E-key inventory screen:** operator screenshot of Red Zone GUI showed the reddish player-inventory strip added above extending wider than the 176px container part, edges not lining up — the region below a chest container apparently isn't sampled at the same fixed 176px width we verified for the container itself, and a second guess at its real width risked the same kind of mismatch round 9 already hit once. Rather than reverse-engineer a second unknown coordinate space, replaced the precise grid there with a borderless, full-canvas-width (0-256, not 176) smooth gradient fill — no hard edges or grid lines to look "wrong shape" regardless of what width vanilla actually samples. Separately, the operator also pointed out the standalone E-key inventory screen (crafting grid + armor slots, `inventory_background()` → `gui/{container,sprites/container}/inventory.png`) was still on the old void-purple palette — recolored it to match (same wood/parchment palette as the chest window), keeping its existing geometry unchanged since that was already confirmed correct in-game (crafting-grid hint box, corner accents). Deployed both the same way (scp + `sudo cp` to `/opt/theglitch/.../gui/{container,sprites/container}/`, `nexo reload`), 0 players online.
- **2026-09-23 — Reskinned 12 of the 29 custom items with icons from 3 operator-supplied packs (`reforge1-icons.zip`/`accessory-icons.zip`/`document-icons.zip`, ItemsAdder+Nexo dual format, 16x16 flat item icons — same drop-in size as the existing textures):** tracked the full 3 kits (36 reforge crystals in 6 colors x big/small x 3 cuts, 9 accessory rings, 9 document/scroll icons) at `assets/icon-kits/{reforge,accessory,document}/`, then picked the best thematic fit per item and overwrote each item's existing `textures/<name>.png` directly — no `.yml` changes needed since `texture:` references stay the same filename. Mapping: `aether_shard`→reforge blue small shard, `rift_crystal`→reforge purple big gem, `void_essence`→reforge purple small (round-cut, distinct from rift_crystal), `void_infusion`→reforge purple small (angular-cut, distinct again), `legendary_relic`→reforge yellow big (turned out to be a gold-ingot shape, good fit), `rune_fragment`→document torn scroll, `rift_reveal_pack`→document letter stack. The 5 "Unidentified" rift items got a 5-ring rarity progression from the accessory pack matching their existing rarity colors: common→plain wood ring, uncommon→emerald ring, rare→lapis ring, epic→diamond ring, legendary→golden ring (an undecorated-to-gilded ring progression reads as "mystery item that becomes gear" well, and rings-as-unidentified-magic-item is a natural fantasy trope). **Out of scope, different asset type:** `loot_crates_*` (Nexo furniture 3D models, not flat item icons — operator confirmed these already have their own custom textures, correctly excluded). **Not touched, not "items" in the sense meant:** `gui_buy`/`gui_close`/`gui_coin`/`gui_sell`/`gui_tab_*` (functional UI buttons, would lose their buy/sell/category meaning if swapped for decorative loot icons). **Not touched per operator instruction:** the 4 key items (already reskinned separately). `nexo reload`-only change (texture swap, no restart), deployed.
- **2026-09-23 (follow-up) — Reskinned the 4 potion items with a 4th operator-supplied pack (`potion1-icons.zip`):** only 2 colors available (blue/red, 3 shapes x 3 sizes = 18 icons total, tracked at `assets/icon-kits/potion/`), so picked by shape/size rather than exact lore-color match: `healing_potion`→red round bottle (mid size, the classic "health potion" silhouette), `corrupted_heal`→red angular flask (large — same red "healing" family as `healing_potion` but a different, sharper shape and bigger size to read as the rarer upgraded version), `aether_tonic`→blue slim bottle (mid, aqua lore color has no exact match but blue is the closest, slim shape reads as "tonic/serum"), `ward_salve`→blue round bottle (small, defensive/warding blue). All 29 custom items are now reskinned except the intentionally-excluded ones above. `nexo reload`-only change, deployed.
- **2026-09-23 (round 10) — Reskinned all 9 LuckPerms rank badges (E050-E058) with a 5th operator-supplied pack (`Mythic Ranks v1.0.zip`):** swapped the procedural 16x16 icons for pre-made Hypixel-style badges (80x16, rank NAME baked into the art — e.g. a small crown icon + "OWNER" text as one image) from the Oraxen-format half of the zip (it also ships an ItemsAdder variant, unused). Mapped by tier, not name, for the 3 paid cosmetic ranks (wisp/stalker/sentinel — Hypixel's own VIP→MVP+→MVP++ donor ladder is instantly recognizable) and by direct name match for the 6 staff ranks: `member`→MEMBER, `wisp`→VIP, `stalker`→MVP+, `sentinel`→MVP++, `helper`→HELPER, `dev`→DEV, `moderator`→MOD, `admin`→ADMIN, `owner`→OWNER. Removed the 9 procedural `rank_*()` functions from `scripts/gen-ui-textures.py`, replaced with a `RANK_ICON_MAP` copy step (same pattern as the chest-window kit) sourcing from `assets/icon-kits/mythicranks/` (all 29 badges tracked — the extra 12 staff/social badges (builder/media/famous/legend/manager/master/mvp/srmod/staff/titan/hero + 7 platform icons) are staged for later use). Bumped the glyph config's `ascent`/`height` from `8`/`9` to `9`/`13` to match the new art's proportions (`server/plugins/Nexo/glyphs/oraxen_glyphs/theglitch.yml`) — same E050-E058 codepoints, so nothing else needed to change. **Deliberately left the LuckPerms prefixes alone** (still glyph + `&color&l[TAG]` text, e.g. `lp group owner meta setprefix ... " &e&l[OWNER] "`) rather than trimming the now-redundant bracket text, since Bedrock clients can't render the PUA glyph at all and would lose the rank tag entirely if it were removed — a little visual redundancy on Java clients (badge picture + text tag) was the tradeoff made to keep Bedrock parity, consistent with the existing convention documented in `theglitch.yml`. Shows in both chat (EssentialsChat prefix) and TAB (tablist/nametag prefix) since both read the same LuckPerms prefix string. `nexo reload`-only change (glyph texture + config, no LuckPerms data touched), deployed.
- **2026-09-23 (round 10 follow-up) — Trimmed the now-redundant `&color&l[TAG]` text from all 9 rank prefixes per operator request:** prefixes are now just the badge glyph (`lp group owner meta setprefix 1100 " "`, etc.) — the badge art already spells the rank name out, so the bracket text was pure duplication on Java clients. Updated `scripts/setup-ranks.sh` (the tracked source of truth for these `lp` commands) and ran the equivalent 9 `meta setprefix` commands live via RCON (LuckPerms is H2-backed, not flat-file, so this needed a live command run, not a file sync). **Known tradeoff, explicitly accepted by the operator:** Bedrock/Geyser clients can't render the PUA glyph at all, so they now see an *empty* rank prefix instead of a readable `[TAG]` — this breaks the "Bedrock always gets a plain-text fallback" convention documented in `theglitch.yml` for every other glyph in this project, but only for these 9 rank prefixes, by explicit choice. No restart needed (LuckPerms prefix changes apply live); 0 players online during the change.
- **2026-09-23 (round 11) — Added a "Mystic" GlitchShops category selling 25 curated weapons from the operator-supplied "Fantasy Weapons" pack (Skulpt's Fantasy Arsenal, Nexo-native, real 3D bbmodel-based custom models under the `skulpt:` namespace — 47 weapons + 4 shields on offer):** picked 25 by name/theme variety across sword/greatsword/spear/glaive/scythe/axe/halberd/hammer types, preferring the `_awakened` variant over its base pair where both existed (6 pairs -> 6 slots saved) and trimming near-duplicate reskins (kept 1 of the 4 "celestial" blades, all 3 of the yin/yang/duality set). New file `server/plugins/Nexo/items/oraxen_items/fantasy_weapons.yml` gives each a real stat package via Nexo's native `AttributeModifiers`/`Enchantments` item components (confirmed exact schema against the [Nexo docs](https://github.com/Nexo-MC/Nexo-Documentation/blob/master/configuration/items/README.md) — top-level keys, not nested under `Mechanics:`) — flat `ATTACK_DAMAGE +6` / `ATTACK_SPEED +0.5` on top of the `NETHERITE_SWORD` base, plus Sharpness VII / Fire Aspect III / Looting IV / Sweeping Edge III / Knockback II / Unbreaking V / Mending — deliberately above the existing Legendary gear ceiling (2 of 4 special attributes, docs/ITEM_BALANCE.md) since these are meant to be a genuine "beyond legendary" prestige tier, not routine loot. Priced in 3 tiers by how "legendary" each name reads: 18k/25k/35k shard buy (10k/14k/20k sell) — for comparison, a max-star godroll Legendary costs ~6100 at the gear vendor, so even the cheapest Mystic weapon costs ~3x that; at ~400/h endgame income (docs/ITEM_BALANCE.md 1) that's 45-87 hours per weapon, intentionally a long-haul chase purchase. Model/texture assets (127 files, including 16 `.mcmeta` animation files some of the chosen 25 needed) copied into `server/plugins/Nexo/pack/assets/skulpt/` (Nexo-native path, not the legacy `external_packs/Oraxen/` one used for older content); full 51-item source kit tracked at `assets/model-kits/fantasy-weapons/` for adding more later. **GlitchShops code changes** (`ShopGUI.java`): added `case "mystic"` to `categoryLabel()`/`categoryTab()` (was silently falling through to the generic "Gear"/`DIAMOND_SWORD` default); added real pagination to the plain-stock `fillStock()` path — 25 items don't fit in the 20-slot `ModernLayout.STOCK_SLOTS` grid (previous behavior for an oversized category: silently truncate, no error, no indication items were missing), so added prev/next arrows at slots 45/53 (same slots GlitchStash's retrieval GUI already uses for pagination) with a `Session.page` field and `page_prev`/`page_next` click actions; this pagination is generic, so any category that grows past 20 items in the future gets it automatically, not just Mystic. Added `mystic` to the end of `config.yml`'s new explicit `tab-order:` list (the 6-tab slot cap at row 1 slots 11-16 already had exactly one free slot — no layout change needed). Verified `mvn -pl plugins/GlitchShops package` builds clean before deploying. This is a **plugin-code change**, not just an asset swap — needs a real jar rebuild + restart on the host, not just `nexo reload`.
- **2026-09-23 (round 11 follow-up) — Fixed 2+ broken Mystic weapon icons and restored the missing slot-grid boxes on chest GUIs + the E-key inventory:** (1) operator screenshots showed Cursed Spear rendering as a missing-texture checkerboard and Celestial Night Blade as a near-invisible sliver. Root cause, confirmed against [Nexo's own docs](https://github.com/Nexo-MC/Nexo-Documentation/blob/master/configuration/items/item-appearance.md): Minecraft 1.21.6+ (we run Purpur 26.2) clips any item model geometry that extends past a standard 16x16 GUI bounding box unless the item explicitly sets `oversized_in_gui: true` — these 3D weapon models, unlike a flat 2D icon, routinely exceed that box (a spear especially). Added `oversized_in_gui: true` to all 25 Mystic weapons rather than guessing which ones needed it, since several others shared the same "texture 2x the model's declared texture_size" pattern as the two broken ones without (visibly) breaking — not worth the risk of leaving a third one broken from an under-scoped fix. (2) Separately, the operator pointed out both the chest-GUI player-inventory strip (flattened to a plain borderless fill in round 9 to fix a width mismatch) and the standalone E-key inventory screen (never had one, in either the old purple or new wood version) were missing visible slot-box grid lines entirely — items floated on a flat color field. Restored a grid for both: the chest-GUI strip now tiles its 18px grid across the *full* 0-256 canvas width instead of stopping at a specific column count, so it has no "this is where it should end" edge to be wrong about regardless of the still-unconfirmed real sample width (same fix philosophy as round 9, just additive instead of subtractive); the E-key screen got real armor-column + main-inventory + hotbar grid lines added at the standard, long-stable vanilla `survival_inventory` coordinates (this screen's own geometry was never in question, only the missing art). Also bumped the container top grid's line opacity (70 -> 140) since it read as too faint in the operator's screenshots. `nexo reload`-only change (asset + item-config, no Java touched), deployed.
- **Round 11 follow-up 2 (2026-09-23, fc9c0ea):** Cursed Spear and Celestial Night Blade still rendered as missing models even with oversized_in_gui, so they were replaced by Violet Splitter and Engine Blade (same price tiers; live shops.yml patched). E-inventory texture: removed the title underline that cut through the armor column, moved crafting 2x2/output/offhand boxes to vanilla coordinates, fixed the off-by-one that dropped the rightmost grid edge. Deployed + restarted.
- **Round 11 follow-up 3 (2026-09-23, 404fffc):** chest-GUI player-inventory strip boxes redrawn at vanilla coordinates. ContainerScreen blits that strip from a fixed texture region (0,126 176x96) regardless of row count; main rows from y=139, hotbar y=197. Container grid's missing right edge restored.
- **2026-09-23 — New plugin GlitchQuests (cf2771b, a0ebc60):** `/rewards` (daily login streak, 7-day cycle, missing a day resets; weekly reward per ISO week; monthly reward) and `/quests` (5 daily quests drawn per day from a pool of 12, 3 weekly from 7, seeded by date so everyone shares the set; all-dailies completion bonus). Tracks kills, MythicMobs kills (reflection, `isMythicMob(Entity)`), VelKoth extractions (reflective `KothWinEvent` hook), mining/ores, distinct containers opened, playtime, travel (statistics delta), fishing, eating, crafting. Rewards are Shards (Vault) + Nexo keys. Per-player YAML in `plugins/GlitchQuests/players/`. Menus are 3-row chests with full-window title-glyph backgrounds from the GUI pack (`rewards_template`, `jobs_template` re-lettered QUESTS) — font `theglitch:menus`, generated by `scripts/gen-menu-backgrounds.py`; template (40,83) = window corner. MythicDungeons also registers `/rewards`; GlitchQuests re-points the bare label at startup (dungeon rewards keep `/drewards`). Timezone = host (Etc/UTC). Admin: `/questadmin reload|list|reset|progress`. In-game visual check pending.
- **2026-09-23 — Core server plugins added (live-only jars):** ViaVersion + ViaBackwards 5.12.0 (Hangar release, sha256-verified) with `block-versions: ["<1.21.4"]` — older clients can't render the Nexo item_model pack, so they get a clear update message instead. GrimAC 2.3.74-8eb5f28 (Modrinth; Hangar's download redirects there) left on its default alert/log-only punishments (no kicks/bans) until false-positive rates with GlitchClasses abilities / MythicMobs knockback are known; `server-name: TheGlitch`. Grim warns that ViaBackwards on 1.21.2+ can glitch vehicles for older (1.21.4–26.1) clients — accepted. CoreProtect: the free 24.0 release refuses 26.2, so it's built from PlayPro/CoreProtect master (has `Bukkit_v26_2`) via `scripts/build-coreprotect.sh` with `project.branch=development` (CoreProtect labels dev builds as possible data-corruption risk — replace with an official release once one lists 26.2). DuckDB storage. Staff perms in `scripts/setup-ranks.sh`: grim.alerts(+enable-on-join) for all staff, grim.verbose moderator+; CoreProtect inspect/lookup helper+dev, +rollback/restore/teleport moderator, `coreprotect.*` admin/owner; `/achievements` for everyone. Spark is built into Purpur (`/spark`). Configs tracked in `server/plugins/{ViaVersion,GrimAC,CoreProtect}/`.
- **2026-09-24 — Performance pass (host is 2 vCPU):** (1) **Scatter freeze:** Watchdog logged 49 dumps in 10h — every 31-min cycle froze the main thread 10-15s in `ScatterManager#placeNew`'s `world.getChunkAt` (~2500 sync chunk loads per cycle: 1248 clears + 1248 placements across 3 red worlds). On Paper/Purpur `scatterNow()` now runs `AsyncScatter`: chunks load via `getChunkAtAsync(x,z,false)` (never generates) with ≤8 in flight, each clear/placement runs on the main thread inside its chunk's load callback, and failed placement attempts retry in rounds (up to `max-attempts-per-container`). The old synchronous path is kept for Folia only. `/glitchcontainers scatter` now reports "started" (result goes to console). (2) **littleroom healthbar pack:** the global listener ran `check_entity_health @EIW` synchronously every 5 ticks over every entity in each world; now scans `@LivingInRadius{r=32}` around each player (bars only render within 32 blocks anyway). Removed the sneak-to-fade feature — it ran `data merge entity` as the text display, which Minecraft broadcasts to ops ("[Text Display: Modified entity data of Text Display]" chat spam). `get_owner_health_difference` threw "Couldn't execute skill" ~10x/s per bar (82k of 92k log lines in 10h); its vars were created INTEGER but written FLOAT — now FLOAT from init (unconfirmed until a live mob is hit). Memory note: the 8G heap is pre-touched (`-Xms8G -XX:+AlwaysPreTouch`), so spark's 4-6 GB "used" is normal G1 behaviour, not a leak.
- **2026-09-24 — Host migration:** live server moved from Oracle Always Free (Ampere A1 ARM64, 2 OCPU/12GB) to a Skrime KVM VPS at `94.249.187.125` (Ryzen 9 9950X, 4 vCPU/16GB DDR5, x86_64, Ubuntu 24.04). Same layout (`/opt/theglitch/server`, repo at `/home/ubuntu/TheGlitch`, `theglitch.service` + backup timer), heap raised to 10G, ufw allows 22/25565/19132-udp only, fail2ban + 2G swap. Cut over after a stopped-server final rsync; all 41 plugins enable identically. Oracle host `217.142.189.253` is stopped and its service/timer disabled, files left intact.
- **2026-09-24 — Hub leaderboards:** installed ajLeaderboards 2.11.0-b338 + DecentHolograms 2.10.1 (Modrinth, added to `bootstrap.sh`) and the PAPI `statistic` expansion. `scripts/setup-leaderboards.sh` creates 5 boards (`vault_eco_balance`, `glitchclasses_level`, `statistic_mob_kills`, `statistic_player_kills`, `statistic_hours_played`) and a top-10 hologram each (`lb_money`, `lb_level`, `lb_mobkills`, `lb_pvp`, `lb_playtime`) near the hub warp; place with `/dh hologram movehere <name>`. Boards `enable-dontupdate-permission: false` (ops implicitly held `ajleaderboards.dontupdate.*` and were never added); Vault + statistic boards refresh offline players hourly and on startup, class level fills as players join. DecentHolograms logs an NBT-API "unsupported version" warning — only affects item lines, unused here.
- **2026-09-25 — Unified menu look + hub panel fixes:** every chest GUI now uses a textured `theglitch:menus` background glyph (new `GlitchCommon` `MenuTitles`; art from `scripts/gen-menu-backgrounds.py`, which now has A–Z banner letters and builds HIDEOUT/RED ZONE (3-row), CLASS (5-row), CRAFTING/STASH/ARMORY/BAZAAR/CLASSES (6-row) from the kit `generic_*` templates). Glass-pane fillers removed. `/hideout` is a 27-slot menu with one card per station (left-click use, right-click upgrade) — the duplicate use-buttons are gone; workbench recipes stay inside columns 1–7. Shop: BAZAAR background, 21 stock slots, stock lore trimmed to 2 lines + price, no floating banner on open. Floating panels (Hideout/Insurance/Bazaar/Stash/Classes): icons now billboard like the labels (duplicate yaw rotation removed); paid clicks (station upgrade, insure held, bazaar buy) ask a chat [YES]/[NO] (`GlitchCommon` `ChatConfirm`, 30s, single use); hub players get +2 entity-interaction range (`PanelReach`, registered by GlitchHideout; hub is PvP/mob-free); `/<x>ui panel here` uses the exact player position and refuses to overlap another panel (`force` overrides). `InsuranceManager.insureItem` now checks the withdraw result. Hideout, Shops, Insurance, Classes and Quests now shade GlitchCommon.
- **2026-09-25 — Economy / dupe audit fixes:** (1) **Free extraction via /spawn**: default players have `essentials.spawn/warp/tpahere`, and RaidListener treated any red→hub world change as an extraction (payout + keep inventory). In-raid players can now only leave a red world by extracting, dying or being moved by the raid system (`RaidManager.exitAllowed`; `glitchraid.admin` bypasses); a red→hub move while still in a raid ends it as MANUAL with no payout. (2) **KOTH party stash dupe**: `handleKothWin` re-stashed the winner (GlitchStash already does) and stashed members with `getContents()` (armor+offhand counted twice) — winner skipped, members use storage contents. (3) **Double-paid loot**: extraction paid item loot value as shards *and* the items were kept (sellable) — buy→carry in→drop/re-pick→extract→sell printed money. Payout is now the kill bounty only (`RaidSession.bounty`), reset after paying (re-entering the same global cycle re-paid old loot); player-thrown items no longer count as loot. Summaries show what was actually paid; non-extracted endings say "no payout" (they used to display a payout that was never deposited). (4) **Residual bonus** (GlitchStash) is capped at this raid's loot value instead of the whole inventory. (5) **Insurance claim dupe**: `/insurance claim` returned a stored copy of the insured item while the player still held it — claims disabled (insured gear is auto-kept on death). (6) **Stash armor loss**: merging a second extraction replaced previously stashed armor/offhand — now folded into contents. (7) **Hideout storage**: no way to deposit — clicking your inventory while Stash/Armory is open now stores the item. (8) **Quest claims** persist immediately (a crash before the 5m autosave re-opened claimed rewards).
- **2026-09-25 — Alpha safety pass:** whitelist stays ON (`enforce-whitelist`; add testers with `/whitelist add`, Bedrock via `/fwhitelist add`). **Default rank** lost `coins.command.drop` (admin coin spawner), all `essentials.warp*` (`/warp red`/`mobtest` dropped players straight into glitch_red; no NPC uses warps; `per-warp-permission: true` now), `essentials.back.ondeath` (/back onto your Red Zone corpse), `essentials.tpahere`, `essentials.chat.format` and dead kit nodes. **Staff**: helpers get tempban + notify + vanish and no fly in red worlds; moderators get ban/unban/banip/invsee/enderchest.others/socialspy/tphere/fly/grim.spectate (moderators previously could not ban at all). Essentials `min-money: 0` (was -10000) and `economy-log-enabled: true`. WorldGuard: `hub_nether` had no protection (now passthrough/pvp/tnt/explosions/lighter/fire-spread deny); hub also denies lighter/fire-spread/lava-fire. Ender chests are blocked in red worlds (loot smuggling). GrimAC stays alert-only (class abilities would false-flag) with alerts on join for staff; CoreProtect logging all block/container/item/chat/command events. Host: SSH is key-only (`PasswordAuthentication no`, root `prohibit-password` — the root password had been exposed in a screenshot), backup timer was never started after the migration (last backup 2026-09-20) — enabled and a fresh backup taken. Rank scripts (`setup-essentials/coins/luckperms/ranks.sh`) updated to match.
- **2026-09-25 — Removed the unused `glitch_pve` world:** never used for gameplay (dungeons moved to MythicDungeons instances). `mv remove`'d live; world folder (65M) and its WorldGuard data moved — not deleted — to `/opt/theglitch/removed-worlds-2026-09-25/`. Dropped from setup-worlds/reapply-world-config/setup-imported-worlds, the TAB `glitch-pve` scoreboard, the `pve_staging` warp, and the GlitchClasses/Insurance/Items world lists (code defaults now = the three Red Zone maps). Deleted the dead glitch_pve dungeon-shell scripts (`build-staging.sh`, `build-dungeon-slot1.sh`, `setup-dungeon-regions.sh`, `recover-worlds.sh`) and `server/world-overrides/glitch_pve/`. Next: operator-supplied dungeon maps imported into MythicDungeons.

## Highest-Priority Remaining Work

1. In-game playtests per docs/TESTING.md — verified in-game 2026-09-01: dynamic capture inside `CuboidRegion y-1..y+4` at all 3 points + ring particles, hub divider, hub `Ping`/`TPS`, container keys (`need-key` vs open). Still open: variant-key arming (+5%/+10%), locator-bar waypoints at distance, `/sb` toggle, `BELOW_NAME` stacks, `NOTCHED_10` bar, plus GlitchRaid (`%glitchraid_*%` + Folia teleport), GlitchInsurance, GlitchEvents, abilities/ultimates, GlitchHideout, spawn areas.
2. Static Fast/Silent arenas (`extract_fast` 15s / `extract_silent` 10s) remain creatable via `/koth create|set time` and mirrored into `extraction-variants.zones` for non-dynamic tests; verify with `/extractadmin zones|armed`.
3. Finish the item loop: Identifier NPC flow.
4. Anti-grief remainder: friendly-fire off everywhere, 2-min AFK kick (shards account-bound DONE `server/plugins/Coins/config.yml:85` + live reload; playtest confirm no loss on death).
5. Configure MythicDungeons (replaces GlitchDungeons, removed 2026-09-22 — see dated entry below) once the operator provides a pre-built configuration.
6. Build or provision the physical maps and dungeon/POI content.
7. Run Bedrock, extraction regression (pyramid/step reject), class, economy, and performance tests.
8. Complete backup, moderation, load-test, and launch work.

## Documentation Rules

- `ROADMAP.md` is the progress checklist.
- `docs/TESTING.md` is the live-server test checklist (run items after each deploy).
- `README.md` is the operator quickstart.
- `HANDOFF.md` is a temporary session handoff and must not contradict this file.
- Design documents describe intended behavior and must label features that are not implemented.
- Live-only state must be identified explicitly: world saves, generated VelKoth arenas, player data, and deployed jars.
